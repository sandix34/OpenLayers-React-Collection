---
name: Fond de carte ol Stamen Terrain
menu: Les couches SIG
---

import { Playground } from "docz"
import FondCarteStamenTerrain from "./FondCarteStamenTerrain"

OpenLayers implémente directement plusieurs fonds de carte dans son API, ainsi on dispose des sources de données tuilées suivantes : BingMaps (avec clé), Stamen, CartoDB (avec clé) et OpenStreetMap (OSM).

<FondCarteStamenTerrain />

```jsx
import React, { Component } from "react"

// classes nécéssaires pour afficher la carte
import Map from "ol/Map"
import View from "ol/View"
import TileLayer from "ol/layer/Tile"
import "ol/ol.css"

// Source de couche pour le serveur de tuiles Stamen.
import Stamen from "ol/source/Stamen"

class FondCarteStamenTerrain extends Component {
  constructor(props) {
    super(props)

    this.state = {
      center: [0, 0],
      zoom: 2,
    }

    // Stamen Terrain
    this.StamenTerrain = new TileLayer({
      source: new Stamen({
        layer: "terrain",
      }),
    })

    // Déclaration de la carte
    this.olmap = new Map({
      target: null,
      layers: [this.StamenTerrain],
      view: new View({
        center: this.state.center,
        zoom: this.state.zoom,
      }),
    })
  }

  componentDidMount() {
    // Défini l'id de l'élément dans lequel la carte est rendue.
    this.olmap.setTarget("map10")
  }

  render() {
    return <div id="map10" style={{ width: "100%", height: "360px" }}></div>
  }
}

export default FondCarteStamenTerrain
```
