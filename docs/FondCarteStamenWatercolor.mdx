---
name: Fond de carte ol Stamen Water Color
menu: Les couches SIG
---

import { Playground } from "docz"
import FondCarteStamenWatercolor from "./FondCarteStamenWatercolor"

OpenLayers implémente directement plusieurs fonds de carte dans son API, ainsi on dispose des sources de données tuilées suivantes : BingMaps (avec clé), Stamen, CartoDB (avec clé) et OpenStreetMap (OSM).

<FondCarteStamenWatercolor />

```jsx
import React, { Component } from "react"

// classes nécéssaires pour afficher la carte
import Map from "ol/Map"
import View from "ol/View"
import TileLayer from "ol/layer/Tile"
import "ol/ol.css"

// Source de couche pour le serveur de tuiles Stamen.
import Stamen from "ol/source/Stamen"

class FondCarteStamenWatercolor extends Component {
  constructor(props) {
    super(props)

    this.state = {
      center: [0, 0],
      zoom: 2,
    }

    // Stamen Watercolor
    this.StamenWatercolor = new TileLayer({
      source: new Stamen({
        layer: "watercolor",
      }),
    })

    // Déclaration de la carte
    this.olmap = new Map({
      target: null,
      layers: [this.StamenWatercolor],
      view: new View({
        center: this.state.center,
        zoom: this.state.zoom,
      }),
    })
  }

  componentDidMount() {
    // Défini l'id de l'élément dans lequel la carte est rendue.
    this.olmap.setTarget("map11")
  }

  render() {
    return <div id="map11" style={{ width: "100%", height: "360px" }}></div>
  }
}

export default FondCarteStamenWatercolor
```
